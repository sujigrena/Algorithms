def InsertionSort(A):
  for j in range(1,len(A)):
    temp=A[j]
    i=j-1
    while(i>=0 and A[i]>temp):
      A[i+1]=A[i]
      i-=1
    A[i+1]=temp


#Function Call
a=[5,4,3,2,1]
insertionSort(a)
print(a)
